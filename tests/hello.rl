! Welcome! I recommend you read the README first to get a feel for the
! syntax of the language. The '!' defines a comment so anything after it
! on the same line will be ignored by the interpreter.

! This simple program displays the message "hello, world!"
! to stdout, then it exits with status 0.

print "hello, world!"

leave 0